# Environnement
APP_ENV=dev

# Redirect uri pour oidc-spa
REDIRECT_URI="/"
# API éditeur
API_URL=
# Lien vers le viewer
VIEWER_URL= "/voir-une-carte/"
# Information de connexion à l'IAM (Identity and Access Manager)
IAM_URL=https://url.to/openid-connect-endpoint
IAM_REALM=
IAM_CLIENT_ID=registered_app_name

VITE_OIDC_USE_MOCK=